# خطة تطوير شاملة — تطبيق "مكسبي" (Maksabi)
### نظام تتبع الأرباح — Flutter

## 1. نطاق المشروع (Scope)

- إدخال دفعات رأس مال (Capital Entries) بتواريخ متعددة
- إدخال دفعات مبيعات (Sales Entries) دورية: يومي / أسبوعي / شهري
- حساب تراكمي للربح: `الربح = إجمالي المبيعات − إجمالي رأس المال`
- حقل اختياري للمصاريف التشغيلية (لفصل الربح الإجمالي عن الصافي لاحقاً)
- بدون تتبع منتجات أو مخزون — تصميم مبسط بالكامل

---

## 2. المعمارية التقنية

**Stack المؤكد:**
- Flutter (كود base واحد لكل المنصات)
- State Management: Riverpod أو Provider
- Local Storage: SQLite عبر **drift** — الخيار الأنسب هنا بدل Hive لأنه يدعم كل المنصات (بما فيها Web/ChromeOS) بنفس الـ API، ومناسب أكثر للبيانات المالية المهيكلة (queries، تجميع حسب فترة، إلخ)
- لا حاجة لأي backend — كل البيانات محلية 100% على الجهاز

**دعم المنصات (Platform Support):**

| المنصة | طريقة التشغيل | ملاحظات |
|---|---|---|
| Windows | Flutter Desktop (native) | drift يدعمها بشكل كامل |
| Android | Flutter Mobile (native) | |
| iPhone (iOS) | Flutter Mobile (native) | يحتاج حساب Apple Developer للنشر على App Store |
| Chrome OS | نسختين ممكنتين: (1) تطبيق Android عادي يشتغل عبر دعم ChromeOS للأندرويد، أو (2) Flutter Web/PWA | الخيار (1) أبسط وبيستخدم نفس ملف الـ APK/AAB بدون تعديل |

⚠️ **نقطة مهمة بخصوص التزامن:** بما إن قاعدة البيانات محلية على كل جهاز بشكل منفصل، البيانات **لن تكون متزامنة تلقائياً** بين Windows وAndroid وiPhone — كل جهاز بياناته مستقلة. لو صاحبك يحتاج يشوف نفس البيانات من أكثر من جهاز، هذا يتطلب لاحقاً إضافة مزامنة سحابية (مثلاً Supabase أو Firebase) فوق القاعدة المحلية. إذا كل جهاز مستقل تمام (مثلاً جهاز واحد بس)، ما فيه أي مشكلة والتصميم الحالي كافي.

**بنية الملفات المقترحة:**

```
lib/
 ├── models/
 │    ├── capital_entry.dart
 │    ├── sales_entry.dart
 │    └── expense_entry.dart (اختياري)
 ├── services/
 │    ├── storage_service.dart
 │    └── profit_calculator.dart
 ├── providers/
 │    └── entries_provider.dart
 ├── screens/
 │    ├── dashboard_screen.dart
 │    ├── add_capital_screen.dart
 │    ├── add_sales_screen.dart
 │    ├── history_screen.dart
 │    └── settings_screen.dart
 └── widgets/
      ├── summary_card.dart
      ├── profit_chart.dart
      └── entry_list_tile.dart

assets/
 └── icon/
      └── logo.png   ← مكان ملف الشعار (1024x1024 px، خلفية شفافة PNG)
```

**ملاحظة عن الشعار:** بعد وضع `logo.png` في `assets/icon/`، يُستخدم مع حزمة [`flutter_launcher_icons`](https://pub.dev/packages/flutter_launcher_icons) لتوليد أيقونات التطبيق تلقائياً بكل المقاسات المطلوبة لـ Windows وAndroid وiOS وChromeOS من نفس الملف الواحد.

---

## 3. نموذج البيانات (Data Model)

```dart
class CapitalEntry {
  final String id;
  final DateTime date;
  final double amount;
  final String? note;
}

class SalesEntry {
  final String id;
  final DateTime date;
  final PeriodType period; // day, week, month
  final double amount;
}

enum PeriodType { day, week, month, custom }
```

---

## 4. منطق الحساب (Core Logic)

```dart
class ProfitCalculator {
  static double totalCapital(List<CapitalEntry> entries) =>
      entries.fold(0, (sum, e) => sum + e.amount);

  static double totalSales(List<SalesEntry> entries) =>
      entries.fold(0, (sum, e) => sum + e.amount);

  static double netProfit(double sales, double capital) =>
      sales - capital;

  static double profitMargin(double profit, double sales) =>
      sales == 0 ? 0 : (profit / sales) * 100;
}
```

---

## 5. الشاشات (UI/UX)

| الشاشة | الوظيفة |
|---|---|
| Dashboard | بطاقات: إجمالي رأس المال، إجمالي المبيعات، صافي الربح، نسبة الهامش + رسم بياني للاتجاه |
| Add Capital | نموذج إدخال دفعة رأس مال جديدة (مبلغ، تاريخ، ملاحظة) |
| Add Sales | نموذج إدخال مبيعات لفترة (مبلغ، نوع الفترة، تاريخ) |
| History | سجل كل الإدخالات مع فلترة حسب النوع والتاريخ، وإمكانية تعديل/حذف |
| Settings | العملة، نوع الفترة الافتراضي، تصدير بيانات (CSV/PDF) |

---

## 6. مراحل التطوير (Roadmap)

| المرحلة | المحتوى | المدة التقديرية |
|---|---|---|
| 1 | إعداد المشروع + النماذج + Local Storage | 2-3 أيام |
| 2 | منطق الحساب + Providers | 1-2 يوم |
| 3 | شاشات الإدخال (Capital/Sales) | 2-3 أيام |
| 4 | Dashboard + الرسم البياني | 2-3 أيام |
| 5 | شاشة السجل (History) + تعديل/حذف | 2 أيام |
| 6 | التصدير (CSV/PDF) + الإعدادات | 1-2 يوم |
| 7 | اختبار + تلميع UI على كل منصة (Windows/Android/iOS/ChromeOS) | 3-4 أيام |

**الإجمالي التقديري: ~3-4 أسابيع عمل فردي** (زادت بسبب اختبار 4 منصات بدل واحدة)

---

## 7. نقاط تحتاج قرار قبل البدء بالتنفيذ

- هل يريد نسخة احتياطية سحابية أو محلي بالكامل يكفي؟
- هل يحتاج أكثر من مستخدم/متجر على نفس التطبيق؟
- هل يبي تصدير تقارير (PDF/Excel) من البداية أو لاحقاً؟
